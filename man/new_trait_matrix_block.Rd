% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trait_matrix.R
\name{new_trait_matrix_block}
\alias{new_trait_matrix_block}
\title{Trait matrix heatmap}
\usage{
new_trait_matrix_block(traits = NULL, breeds = character(), ...)
}
\arguments{
\item{traits}{Character vector of trait columns to show. Default: all eleven
1-5 traits.}

\item{breeds}{Character vector of breed names to keep. Default: empty, which
shows every breed.}

\item{...}{Forwarded to [blockr.ggplot::new_ggplot_transform_block()].}
}
\value{
A `trait_matrix_block` object.
}
\description{
A breeds-by-traits matrix: one tile per (breed, trait), filled by the 1-5
score, in the spirit of a ratings heatmap (rows ordered by mean score so a
gradient emerges). Two selectors drive it: which `traits` to show (columns)
and which `breeds` to keep (rows; none selected = all). Pivots the wide trait
columns to long form internally, so it takes the raw breed table straight in.
The plot fills the panel height (`block_ui` below), so the rows stay legible
even with every breed shown.
}
